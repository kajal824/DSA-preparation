class Solution {
    public int maxProduct(int[] nums) {

        Arrays.sort(nums);

        int n = nums.length;
        int i = nums[n-2];
        int j = nums[n-1];

        int mx = (i-1)*(j-1);

        return mx;
        
    }
}